{
  shortcuts = {
    captureActiveWindow = null; # 捕获当前活动窗口
    captureCurrentMonitor = null; # 捕获当前显示器
    captureEntireDesktop = null; # 捕获整个桌面
    captureRectangularRegion = "Meta+Ctrl+a"; # 捕获矩形区域
    captureWindowUnderCursor = "Meta+Ctrl+s"; # 捕获光标下的窗口
    launch = null; # 打开应用
    recordRegion = null; # 录制区域
    recordScreen = null; # 录制屏幕
    recordWindow = null; # 录制窗口
  };
}
